# Changesets

This folder is used by Changesets to manage versioning and publishing of the packages in this monorepo.

- Run `npm run changeset` to create a new changeset.
- Run `npm run release` to version and publish all pending changesets.
